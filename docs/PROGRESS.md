# Project Progress

Updated: 2026-09-13 (Asia/Shanghai)

## Latest

- 当前角色：Codex 决策 Agent。用户授权逐项优化协作流程；第二片 MCP 交付摘要与补读代码、独立复核已完成，真实宿主加载待重连冒烟。
- DeepSeek #42 开发、Kimi #43 复核发现结构化报告未绑定实际任务号；DeepSeek #44 修复、Kimi #45 最终复核通过。四项成果均已收取完成。
- 新增只读 `talk_get_delivery`：默认摘要区分 runner/工作流/业务自报；报告 schema 与实际任务号匹配才采用结论，旧文本 unknown、错号/非法报告 invalid。不会自动 collect/accept，不改变旧工具默认输出，不按消息路径读取本机文件。
- 摘要文本最多 1200 字符、正常 JSON 最多 6000 字符；阻塞/未完成优先，省略信息与补读参数可见。detail 可分页取服务端已存原文或指定结构化字段；携带结果消息 ID 与 expect_sha256 防止混接变化内容。不能恢复上游已截断的内容，不能只凭摘要验收。
- Kimi 独立运行 140 项定向测试及 13 项专项断言通过；真实匿名管道 stdio 0 次退化。之前 9451 字符报告 10 页重建 hash 相同；本机真实宿主尚未加载新工具，不宣称端到端已验收。
- 本片基线 `881ce12`，分支 `codex/terminal-return-codex`；文档与实现统一提交、推送该已授权分支，不合并 main。
- 2026-09-13 恢复核对：昨日落盘后尚未提交推送，HEAD仍为881ce12、12个本片文件待提交；本轮补齐编码/差异检查与统一提交推送。当前五小时已用1%、周已用25%，昨日96%的门禁状态已过期；本轮只完成收尾，不启新片。新工具仍不在当前宿主工具目录中。

## 已完成基线与边界

- 第一片本地交付 CLI `scripts/talk_workflow.py`、模板及规范已落地，#38–#41 已收取，独立 63 项测试通过。`--expect-task-id` 必须按报告提交者本次实际任务号使用。
- #43 复核原报告误写 `review-42` 被本地校验拦截；Codex 核对任务43结果消息中的报告路径后，仅生成任务号校正副本 `review-task43.json`，原件和 FAIL 证据保留；没有跳过校验。
- #37 清理已完成：416 个本地文件保留，误加临时材料从未发布历史中排除；备份引用仅本地，已发布历史未重写。
- 十分钟等待和角色输出修复已完成重连验收，无需重跑长等待。执行者预算 3600 秒、等待 600 秒、MCP 660 秒；日常只传当前 task_ids。摘要最多20条，完整ID仍O(N)。
- 取消/断线恢复、外部唤醒、等待期间零模型回合未实现或证实；宿主续行与无变化更新仍存在。本轮未启停服务/改用户配置/浏览器验证。
- pi TypeScript 扩展未加入新工具；最终用户页面未变化，USER_MANUAL 不变；临时交付报告不入库。

## 默认协作

- 前端 Kimi 开发/DeepSeek 复核，后端相反；Codex只范围、短摘要、分歧及文档/Git收尾。
- 按 TASK_WORKFLOW.md 使用交付包；MCP新工具加载后优先只读摘要，有相关省略按稳定引用补读；未加载时本地CLI只读摘要。格式校验与任务号匹配不代表业务正确。
- 原始字符/输出 token 标记不能直接换算实际额度；不量化尚未实测的节省。

## 下一步

1. 额度已恢复；重连 TALK MCP 后确认目录出现 talk_get_delivery；只做一次指定已完成任务的只读摘要与分页/只读性冒烟。现有旧报告是自由文本时应为 unknown，不当失败或强行迁移。无需重跑十分钟等待。
2. 宿主入口确认后，再决定下一优化片；本轮只补昨日未完成的提交推送，不自动启动开发。
3. 群聊会议轮次与原终端回传仍为产品后续方向，见 BRAINSTORM_NEXT.md；本轮未开发。

恢复指令：`继续项目`。完整过程见 PROGRESS_HISTORY.md。
