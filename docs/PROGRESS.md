# Project Progress

## Latest

- **界面批次已推送并创建 PR #3**：用户于 2026-09-08 明确回复“可以推送，然后下一步做什么”。任务 / 群聊 / 角色导航与 80% 默认密度已同步 GitHub，当前优先事项是分支集成与版本收尾，不开启新功能。
- 左侧“任务 / 群聊 / 角色”同级切换，群聊只列普通房间，任务对话从任务详情进入；主任务归组、成果与下一步优先、原始说明收进“任务要求”，普通详情移除内部技术字段。
- 已修复 #15 黑板切到 #9 Hall 后沿用旧树的问题，以及完成态显示暂停/终止、受限成果入口无反馈、移动端标题栏遮挡和刷新丢焦点等问题。

Updated: 2026-09-08 (Asia/Shanghai)

## Current Snapshot

- 当前分支：`codex/ui-workspace-v1`，从 `codex/task-hall` 的 `f60c077` 创建；Codex 为决策 Agent。用户已确认原预览与 80% 桌面密度方向，本次另行明确授权增加群聊标签、移除旧列表。
- 已公开推送 `codex/ui-workspace-v1`，功能提交 `df3877a` 已通过 `git ls-remote` 核对。PR #3：`https://github.com/bobo506/TALK/pull/3`，base 为 `codex/task-hall`。此前自动审批拦截已由本次明确授权解除，没有修改 GitHub 权限或审批策略。
- TH-6d 根任务 #15 与子任务 #16/#17/#18 仍为已验收、已完成基线；本切片未修改这些任务、发送消息或启动新的任务执行。
- 当前拓扑仍为 Codex / DeepSeek Harness / 官方 Kimi Code CLI。只查看历史页面不需要启动其它角色服务；交办新的执行任务时才需要相应 bridge。
- 本地服务已后台启动，入口 `http://127.0.0.1:8000/`。若端口已停止：`.venv\Scripts\python.exe -m uvicorn server.main:app --host 127.0.0.1 --port 8000`。

## Verification

- 本次仅同步已提交代码并补记进度，未修改功能或重跑测试。远端功能提交 `df3877a4eae32914eb2a47ac239068fccd77a620` 已核对，PR #3 已创建。GitHub CLI 返回 401，已使用现有 GitHub 连接器完成 PR 操作，无需用户重新配置权限。
- 最新群聊导航：Node 17 项、Python 页面/群聊接口 4 项通过。浏览器实测三个标签、群聊搜索空结果、创建弹窗与 Escape 焦点恢复、任务 #9 的完整对话、列表刷新；768px/390px 无横向溢出，控制台未捕获错误。本轮未在真实项目创建群聊或发送消息，创建导航通过生产函数回归与隔离 API 测试验证。截图在 `.tmp/group-chat-20260908/`。
- 最新尺寸微调：桌面统一为原界面 80%，补偿视口高度；宽度小于 701px 时沿用原来字号与触控尺寸。1488px 实测标题栏 70→56、搜索框 44→35.2，工作台底边仍为窗口底边；769px 无横向溢出，390px 搜索框仍为 44、缩放为 1。角色/交办弹窗可操作，页面契约 2 项与 `git diff --check` 通过。
- JavaScript 语法检查通过；Node 原生回归 11 项通过，覆盖跨任务迟到响应、错误响应、跨项目/账号、错误树操作拦截和操作后保持选择。
- Python 定向回归 4 项通过：页面契约 2 项，里程碑验收与提交/收取流程 2 项；隔离测试数据库运行。本轮没有重跑历史 389 项全量测试。
- Codex 内置浏览器实测：任务筛选/搜索、角色切换/搜索、项目空状态、角色交办自动选人、Escape 关闭和焦点恢复、成果读取、非成员提示保留、#15→#9 Hall 状态一致、子任务返回主任务。
- 1488×1056 桌面与批准的两张预览分别放入同一次对照输入；390px 任务/角色/弹窗及 769px 对话页核对通过，无页面横向溢出。修复过程中发现的手机标题栏遮挡已复测。
- QA 说明见根目录 `design-qa.md`；本地截图在 `.tmp/workspace-qa-20260907/`，不进入公开提交。最终页面控制台未捕获错误。

## Current Boundaries

- 群聊复用现有房间、成员与消息功能，列表范围为当前项目及历史无项目房间；排除任务专属房间。新建群聊绑定当前项目，无房间时不回落到全局消息流。移除的是旧界面入口，没有删除历史房间、消息或后端 API。
- 创建任务弹窗和完整对话沿用现有流程；任务树高级配置仍有开发术语，本切片没有重做该流程。
- 角色来自项目配置，“正在参与任务 / 当前没有进行中的任务”表示工作状态，不表示 bridge 在线或有可用额度。角色页面没有增加启动服务、改权限或成员管理功能。
- 历史任务的标题、正文保持原始数据；预览中的简短示例名称没有替换真实任务。列表可搜索，完整任务要求默认折叠。
- 当前 QA Tester 账号不是 #15 Hall 成员：可以核对工作进度，查看成果会显示访问提示；已用有权限的 #9 验证真实成果读取。
- 历史审计、生成预览、验收标记与临时辅助文件保留本地，不打包到代码提交。

## Next

1. 优先整理并合并已完成的分支，形成 `main` 稳定基线。当前 `codex/task-hall` 相对远端 `main` 领先 64 个提交；旧 PR #2 的 head `1da4797` 是 `codex/task-hall` 的祖先，本批 PR #3 则在其上继续。先核对整条前置链和合并方式，再统一收尾；本轮没有授权自动合并或关闭旧 PR。
2. 合并前在集成分支跑完整回归，并使用新版工作台跑一次完整真实验收：新建普通任务→负责人接手→提交/检查→收取完成，并核对普通群聊创建与消息收发。这是下一步建议，本轮没有执行或新增真实任务。
3. 稳定基线后再确定下一开发切片；TH-7 普通终端接入包装的最小范围尚待确认。创建弹窗进一步简化、完整 Members / Activity、Tester 隔离等保留待办，不自动扩大范围。
4. 恢复指令：`继续项目`；先核对 PR #3 与分支状态，避免重复开发或推送。

## References

- 项目简报：`docs/PROJECT_BRIEF.md`
- 当前模块：`docs/spec/MODULE_webui.md`
- 用户手册：`docs/guides/USER_MANUAL.md`
- 历史记录：`docs/PROGRESS_HISTORY.md`
