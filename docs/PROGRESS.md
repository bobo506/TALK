# Project Progress

Updated: 2026-09-12 (Asia/Shanghai)

## Latest

- 当前角色：Codex 决策 Agent；用户反馈 Git 收尾额度消耗偏高，已授权逐项优化，先做使用流程。
- 使用流程第一片已完成：DeepSeek #38 开发、#40 返修；Kimi #39/#41 独立复核，最终 PASS_WITH_LIMITATIONS。四项成果均已收取完成。
- 新增本地 `scripts/talk_workflow.py`、使用指南与定向测试。交付包校验字段/长度/自报一致性，摘要默认最多 1200 字符并显式标记截断；默认调用带 `--expect-task-id`，避免错用另一任务文件。
- 已修复深嵌套异常、complete 与失败验证矛盾；Kimi 独立运行 63 项测试通过。实际返修及最终复核摘要均为 1110/1200 字符、已截断；开发报告曾写“未截断”，汇总以实际输出及复核为准。
- 已同步 AGENTS 与短任务模板，固定顶层最小派发字段。机械校验只管格式与自报一致性，仍需独立复核；尚未接入 bridge/MCP 自动强制执行，不保证事实完整性或计费节省。
- 本轮已实际使用本地摘要读取交付；等待的宿主续行与无变化更新仍存在。两次派发参数组合 422 已纠正，不宣称整个流程已经低消耗。
- 当前分支 `codex/terminal-return-codex`，本片基线 `aa90682`；代码/文档/进度统一收尾提交并推送同一已授权远端分支，未合并 main。

## 已完成基线与边界

- #37 Git 清理已完成：416 个本地文件保留，误加临时材料从未发布历史中排除；原备份 `backup/pre-cleanup-7b6fbcf` 仅本地，已发布历史未重写。此前远端已核对为 `aa90682`。
- 十分钟等待及角色输出修复已完成独立复核和最终 MCP 重连验收；无需重复重连或十分钟测试。执行者预算 3600 秒、等待上限 600 秒、MCP 660 秒。完整证据见 WAIT_TEST_RECORDS 与历史进度。
- 等待摘要最多 20 条，完整 ID 列表 O(N)；日常显式指定当前 task_ids。取消/断线恢复、外部唤醒、零模型回合均未实现或证实。
- 前端两项改动已落地，真实页面继续由用户验收；Codex 默认不做浏览器检查。本片无用户页面或最终用户操作变化，USER_MANUAL 不变。
- `.tmp`/预览/日志不新增入库；不修改用户级配置、不启停服务、不自动派发新优化任务。

## 默认协作

- 前端 Kimi 开发/DeepSeek 复核，后端反向；Codex 只管范围、短摘要、分歧和进度/Git 收尾。
- 本地交付与收尾按 `docs/guides/TASK_WORKFLOW.md`；开发完成暂停后独立复核，报告按任务号校验，只展开必要证据。
- 原始输出字符或 token 标记不能直接换算实际额度；当前缺少本轮前后可比计费明细，不估算节省比例。

## 下一步

1. 先在后续真实任务中使用本地交付流程，观察主控往返和实际体验，本片完成即暂停。
2. 下一优化片优先确定 MCP 交付摘要及 runner/业务完成区分的最小合同；随后逐项评估等待、续接、取消，不一次铺开。
3. 群聊会议轮次与原终端结果回传仍为产品后续方向，见 BRAINSTORM_NEXT.md，本轮未开发。

恢复指令：`继续项目`。完整过程见 PROGRESS_HISTORY.md。
