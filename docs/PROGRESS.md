# Project Progress

Updated: 2026-09-14 (Asia/Shanghai)

## 当前状态

- 当前 Codex 为决策 Agent；分支 `codex/terminal-return-codex`。N1 统一任务标题开发及独立复核完成：Kimi #58、DeepSeek #59，收尾发现同值纯数字名称被吞后由 Kimi #60 修正，DeepSeek #61 针对性复核 PASS；四项均已收取。
- 新任务在创建事务取得真实 `task.id` 后统一为“编号-任务名称”，Web/MCP/SDK/schedule 共用入口，Hall 名复用；空白回退“编号-未命名任务”，纯数字同值名称保留（如 `60-60`），已规范前缀幂等。历史记录不迁移。
- 最终复核独立通过 88 项 Python、4 项 Node 标题测试与 6 项 API 边界探针；继承 #59 对未改部分的复核证据（50 项 Node 等）。Codex 完成差异空白检查，不重复完整测试。真实页面及受沙箱限制的子进程 E2E 未验证。
- 已同步任务模块合同与用户手册。人工复制旧的带编号标题新建任务仍可能形成“新编号-旧编号-名称”；当前无自动复制路径，不进行破坏性数字清洗。Hall 名沿用 80 字符截断。
- 耗时展示 #54/#55 已提交 `ca8675e`，页面视觉仍待用户验收：执行中每秒更新，结束固定，缺有效起止显示—，重试仅最近一次执行。

- Git 收尾：用户已明确确认 `bobo506/TALK` 为其仓库并授权推送，N1 提交 `2b917ce` 已成功推送至 `origin/codex/terminal-return-codex`；此前自动审批阻塞已解除。

## 验收与下一步

1. 当前共享服务未由本轮重启，不能假定已加载 N1。加载新后端代码后，浏览器 Ctrl+F5；使用已有登录密钥，在任务列表新建普通标题、空白标题，检查列表/详情/Hall 标题、编号和耗时，历史任务保持可读。纯数字同值 ID 已有隔离 API 测试证据，无需在真实库预测编号。
2. 页面实际效果待用户验收；本轮不再启动新切片。下一步为路线第2阶段 L1 原生 Kimi Code/DSH 主控入口验证，分终端逐片推进；WorkBuddy 按 L2 另核本机产品/版本与模型能力。
3. 统一路线见 `spec/DEVELOPMENT_ROADMAP.md`：N1 已完成开发复核，后续主控验证→主被动模式→目录工作区→角色实例/领取/会话隔离→本地进程管理与Web角色启停→群聊会议（第7阶段）→桌面壳及原生目录接入→双工作区/退出验收。先基本功能（含群聊）再桌面封装。
4. 桌面目标仍为仅自动启动 TALK、手动启停工作区角色、A/B 同名角色独立会话、关窗只退出本应用拥有服务；完整隔离未完成，外部独立 Codex/WorkBuddy 不被退出。

## 等待与交付流程

- 用户已采纳#57建议1：AGENTS固化无新输入/异常时只按原运行标识续等，不额外查询、读文件/diff或重新展开方案；必须更新时最短说明，完成/异常/新输入及时处理。仅提示词约束，未量化节省。建议2（降低推理强度）和3（用量归因方案）不推进。
- 当前默认仍由用户通知主控收取，持续等待须用户明确要求。#56部分验证报告已收取，受DSH沙箱限制未运行A/B，Desktop指令替换范围仍未知；#57咨询已收取，本次不继续这些实验。

- 项目外层等待目标600秒仍以宿主能力/更高优先级指令允许为前提；本轮实际exec外层yield=60000ms，内部wait预算600秒。4次请求内部耗时600.032/83.032/600.032/47.734秒，持续主控等待到复核收取完成；不能宣称验证了外层600秒或节省token。
- 此前短程yield600000参数未报错、53.6秒返回；连续600秒阻塞能力未证实。用户级MCP超时660秒、执行者预算3600秒保持，未改全局配置。
- #50/#51完整结构化JSON入服务器与新MCP摘要真实入口已验证；#52/#53调研已收取，二进制原句命中不证明当前加载路径，替换配置能否覆盖开发者层指令未证实。无外部自动唤醒、断线补领或取消立即生效保证。
- 主控按TASK_WORKFLOW摘要读取，必要时按稳定引用补读；schema匹配只是自报，不替代独立复核。前端Kimi开发/DeepSeek复核，后端相反；Codex维护正式文档/Git，不重复全量探索。

恢复指令：继续项目。完整证据见PROGRESS_HISTORY.md。
