# Project Progress

Updated: 2026-09-15 (Asia/Shanghai)

## 当前状态

- 当前 Codex 为决策 Agent，分支 `codex/terminal-return-codex`。L1-1 Kimi Code 独立主控入口准备已收尾：DeepSeek #62 开发（原自报 partial）、Kimi #63 独立复核在声明范围内 PASS，两项已收取。基线 `eeea11d`。
- 已交付无密钥 MCP 模板、配置生成/身份预检/工具探针脚本与启动器，复用普通终端 MCP，未改任务协议。Kimi 复核独立通过 14 项新增测试及 29 项回归，并补齐真实匿名管道九工具、本人 `agent:kimi` 对项目 `prj_e8fe7066bbec` 的只读身份连接、CLI `0.38.0 --version/--help` 实证。
- 尚未激活工作区 `.kimi-code/mcp.json`、未启动真实模型主控闭环。两级配置加载/新会话生效仍需真实会话验证；本人真实外部密钥文件路径尚未跑通（复核使用 runner 注入环境变量）。不得把工具目录/身份预检等同主控闭环完成。
- 两项轻微待办：`--expect-project` 缺省仅核对解析出的项目，需显式提供独立期望值；激活前明确 `.kimi-code/` 的 Git 忽略策略，避免本机路径配置误提交。均不阻塞本片入口准备收取，不自动改全局认证或激活配置。
- N1 已推送 `2b917ce`（进度 `eeea11d`）；#62/#63 创建返回标题已为“编号-名称”，证明服务端已加载命名逻辑。历史不迁移，真实页面整体效果与此前耗时仍未明确验收。

## 新需求与下一步

1. 新增项目级“开发要求”：在前端角色列表编辑，主控派发前读取，包含各角色职责和交叉复核要求，例如 Kimi 负责文字/交互/前端、DeepSeek 负责后端。用户明确只保存最新内容，不做版本号或历史记录；已派发任务按原任务包执行。实现后替代 AGENTS 中这部分分工维护，代码规范等仍留仓库规则。当前仅记录，未开发、未改变现行分工规则。
2. 建议下一独立切片优先实现上述“开发要求”，排在当前 #62/#63 收尾后、主被动模式之前；本轮不自动派发。按后端存储/工具读取与前端编辑划分明确切片，沿现有项目权限，不赋予页面文字覆盖外部系统指令的能力。
3. L1 后续仍需激活 Kimi 独立会话配置与有限协调授权，验证一次无破坏性委派→异身份复核→原会话读取/收取汇总；再分片验证 DSH，WorkBuddy 按 L2 核验。配置步骤见 `guides/TERMINAL_MCP.md`。
4. 路线仍先基本功能（含第7阶段群聊）再桌面封装；完整工作区/角色隔离与生命周期未实现。用户已确认 `bobo506/TALK` 归属并授权推送，收尾沿用现有分支。

## 等待与交付流程

- 用户已采纳#57建议1：AGENTS固化无新输入/异常时只按原运行标识续等，不额外查询、读文件/diff或重新展开方案；必须更新时最短说明，完成/异常/新输入及时处理。仅提示词约束，未量化节省。建议2（降低推理强度）和3（用量归因方案）不推进。
- 当前默认仍由用户通知主控收取，持续等待须用户明确要求。#56部分验证报告已收取，受DSH沙箱限制未运行A/B，Desktop指令替换范围仍未知；#57咨询已收取，本次不继续这些实验。

- 项目外层等待目标600秒仍以宿主能力/更高优先级指令允许为前提；本轮实际exec外层yield=60000ms，内部wait预算600秒。4次请求内部耗时600.032/83.032/600.032/47.734秒，持续主控等待到复核收取完成；不能宣称验证了外层600秒或节省token。
- 此前短程yield600000参数未报错、53.6秒返回；连续600秒阻塞能力未证实。用户级MCP超时660秒、执行者预算3600秒保持，未改全局配置。
- #50/#51完整结构化JSON入服务器与新MCP摘要真实入口已验证；#52/#53调研已收取，二进制原句命中不证明当前加载路径，替换配置能否覆盖开发者层指令未证实。无外部自动唤醒、断线补领或取消立即生效保证。
- 主控按TASK_WORKFLOW摘要读取，必要时按稳定引用补读；schema匹配只是自报，不替代独立复核。前端Kimi开发/DeepSeek复核，后端相反；Codex维护正式文档/Git，不重复全量探索。

恢复指令：继续项目。完整证据见PROGRESS_HISTORY.md。
