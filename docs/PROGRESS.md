# Project Progress

Updated: 2026-09-11 (Asia/Shanghai)

## Latest

- 更新：用户修改默认模型后，DeepSeek #25已独立审查PASS，实测39项Node及2项Python。第二项代码本地收尾，页面效果待用户验收；不再由Codex进行浏览器验证。此前#24到期失败保留为历史。

- 第二项已由 Kimi 在任务 #23 开发并提交，当前分支 `codex/task-chat-layout`，HEAD=a0fe577；代码尚未提交、未收取，等待独立复核与用户页面验收。
- 2026-09-11 派发 DeepSeek 审查 #24，预检失败，尚未审查代码：模型 `deepseek-v4.1-flash-expires-on-0910` 已到期，服务要求改为 `deepseek-flash`。已请用户修改 DSH 默认模型；修改后只续办审查，不重复开发 #23。
- Kimi 报告39项Node、2项Python通过；Codex本轮未重跑、未浏览器验证，不把开发者报告当作独立审查结论。修改范围为workspace.js、workspace.css、index.html及新增tests/workspace_task_chat_layout.test.cjs。
- 新默认流程已写入AGENTS：前端Kimi开发/DeepSeek复核，后端反向；Codex只负责范围/摘要/分歧/收尾。用户要求进一步省额度，本轮起默认不由Codex操作浏览器，页面效果由用户验收。
- 昨日消耗排查：角色列表工具未过滤历史实例，原始输出标记约64718 tokens且夹带旧日志；另有重复背景读取、轮询和浏览器图像/页面树。输出发生截断，原始长度不等于实际计费，无法将用户观察的30%额度精确归因。后续工具层先筛选，禁止整包历史输出。
- 第一项成果开关已提交37d88c7，文档提交a0fe577。远端推送仍未授权，不重试或绕过此前审批拒绝。

## 已完成与验证

- 成果按钮显示“查看成果 / 收起成果”，支持加载中关闭、再次展开；上下文切换与关闭使旧请求失效。补aria属性，保持原权限和文件/撤回/错误呈现。
- Codex独立执行32项Node、2项Python页面测试、两份JS语法和diff检查通过；没有重跑全量后端。
- 真实页面#19展开/收起/再展开、Enter/Space、刷新保持状态、#19与#9切换、空项目往返及#20无权限提示通过。当前浏览器实际379×577，宽屏与真实账号切换未补测；异步竞态与账号变化由行为测试覆盖。详情见design-qa.md。
- Kimi修改5份代码/测试文件；Codex同步用户手册、webui模块和进度记录。临时预览、.tmp、数据库、日志、密钥、用户配置不提交。未启停用户服务。

## 新增已授权的消耗优化（2026-09-11）

- 用户要求修正角色历史列表输出：后续独立小切片由DeepSeek实现、Kimi复核。MCP默认只返回角色摘要与必要的当前实例，不返回全部历史实例/完整last_error日志；历史详情按需查询，并增加返回大小回归测试。当前仅调用侧过滤已落实，工具端尚未改。
- 重复文档读取按会话已读记录与Git变更补读；相同任务不反复读简报/模块全文。等待执行期间不读diff或浏览器，状态只返回id/status/简短错误；长任务采用更低频检查，避免反复启动推理回合。
- 用户修改DSH默认模型为deepseek-flash后，已派发#25续办只读审查#23；#24失败记录保留，勿重复派发。

## 当前验收与下一步

1. 刷新 http://127.0.0.1:8000/ ，用已有账号选一个有权限且已有成果的任务，连续操作“查看成果 → 收起成果 → 查看成果”；切换任务/项目后应恢复收起。已启动服务无需重启。
2. 用户修改DSH默认模型后，续办DeepSeek只读审查#23的实际差异；通过后收取、同步文档并本地提交，页面交用户验收。不得重复派发Kimi开发。
3. 角色列表历史idle/busy心跳误报在线仍未修复，派发前核对真实bridge。Kimi本次长任务达到600秒后不会自动恢复；短续办成功，执行超时与恢复策略尚未改进。
4. 会议方向继续见docs/spec/BRAINSTORM_NEXT.md：同项目同团队复用群、一群一活动轮、人工/原终端主持、明确汇总结束并返回原终端。尚未实现；任务#20的草案须先解决legacy scope与引用兼容。
5. 普通群聊完整连续上下文、回复自动提醒、服务管理/桌面客户端仍未完成，不与本轮小修混做。

## 启动与交接

- 项目根D:/claude-test/TALK；服务 `.venv\Scripts\python.exe -m uvicorn server.main:app --host 127.0.0.1 --port 8000`。本地角色Codex / 官方Kimi Code / DeepSeek Harness。
- TALK本地stdio MCP已实测，以human:bobo派发。桥接命令与配置见docs/guides/TERMINAL_MCP.md及现有启动指南；密钥不入库。
- #20 DeepSeek只读试运行已completed；#22 Kimi开发成果已completed。不要把#21失败当作未完成而重复整项。
- 第一项受测版本已本地提交 `37d88c7`。gh查询PR返回HTTP401；推送 `origin/codex/ui-result-toggle` 被自动审批拒绝，原因为未明确授权向具体远端 `https://github.com/bobo506/TALK.git` 发送非公开代码/文档。未推送、未创建PR、未绕过；等待用户明确授权该目的地。人工验收前不合并main。
- 旧.tmp/th7-terminal-v1等工作树保留，不在旧基线上续开发。完整历史见PROGRESS_HISTORY.md。恢复指令：`继续项目`。
