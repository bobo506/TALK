# Project Progress

Updated: 2026-09-13 (Asia/Shanghai)

## 当前状态

- Codex 为决策 Agent；本轮仅处理重连验收、等待配置核对及默认完整摘要一个切片。基线 `b02127e` 已提交推送，分支 `codex/terminal-return-codex`。
- 重连后的旧版 `talk_get_delivery` 真实入口已通过：任务 #45 / 结果 2521 的旧自由文本结论 unknown；两页共 773 字符与整页完全一致、hash 一致，任务和收取时间未改变。此前“待重连”已完成，不需重跑十分钟等待。
- DeepSeek #46 开发、Kimi #47 初审、DeepSeek #48 返修、Kimi #49 独立最终复核已完成，最终 PASS；155 项定向测试和 17 项独立断言通过，#46–#49 已全部收取完成。初审要求修正显式限长的截断标志、消除重复正文；返修同时补齐原 MCP 响应遗漏的验证证据、限制和下一步。
- 合法结构化报告默认不再按 1200/6000 字符或条目长度机械截断。CLI 输出完整必要内容；MCP 三类正文保留在 preview.items，summary_text 只承载计数/定位及其它必要信息，完整性须按同次响应合看。输入 schema/64 KiB 拒收、任务号绑定、只读与稳定引用分页保持；旧文本预览/显式限长有省略时仍需补读。
- #48 自测 155 项定向回归通过。同夹具 JSON 3990→4466 字符，三类正文重复 13→0，新增长度用于补齐此前缺失的信息；字符数不等于计费 token，不据此宣称节省。
- 当前宿主已加载的是 b02127e 工具；本次新行为与工具描述须再次重连后补一次真实入口验证，隔离测试不能替代。未启停服务、操作浏览器或修改用户配置。

## 等待问题与边界

- 本机 MCP `tool_timeout_sec=660` 是单工具超时；执行者预算 3600 秒、TALK 等待 600 秒保持。未找到可确认关闭外层 functions.wait/模型续行的公开配置，此前主动 yield 不能全归因于宿主强制。
- 项目规则已要求避免无变化播报，但不能覆盖宿主更高优先级沟通要求。外层续等和无变化更新没有配置级消除证据，仍属待解决问题；摘要修改不算这两项已完成。
- 自动外部唤醒、断线补领/去重、取消立即生效和 Windows 子进程生命周期仍未完成或证实。
- 本轮中途共享账号五小时已用 51%、周已用 33%；不是本片独占成本，未触发 90% 门禁；不再开启新切片。

## 默认协作与下一步

1. 本片代码与独立复核完成，收取与3项编码检查通过；统一提交推送当前授权分支，不合并 main。
2. 用户重连新 MCP 后，以一个已存合法结构化结果验证默认完整响应及核心字段，不再跑长等待或创建探针任务。
3. 等待优化需继续确认宿主支持的控制方式或执行约束；不承诺改一个配置即可消除，也不擅自更改模型/推理强度。

前端默认 Kimi 开发、DeepSeek 复核；后端相反。Codex 只读交付摘要、处理分歧及文档/Git 收尾。格式校验和自报结论不等于业务验收；有疑点才补读必要字段。临时交付包不入库，用户页面与 USER_MANUAL 未变化。

恢复指令：`继续项目`。完整记录见 PROGRESS_HISTORY.md。
