# Project Progress

Updated: 2026-09-12 (Asia/Shanghai)

## Latest

- 本轮角色：Codex 决策 Agent。用户授权 DeepSeek #37 整理并推送当前分支；任务已收取完成。未发布的纯临时材料提交 `7b6fbcf` 已整理为 `d323c91`，416 个本地文件全部保留，用户 `.gitignore` 修改保留；待推送历史未新增临时材料、业务代码无差异。
- DeepSeek 完成本地整理后因其沙箱管道限制未能 push；Codex 按既有授权补推，已用 `ls-remote` 核对 `origin/codex/terminal-return-codex = d323c91`。本地备份 `backup/pre-cleanup-7b6fbcf` 未推送。随后补记本轮进度；完整证据见历史记录。

- 用户确认本版先投入使用，剩余优化暂缓；只有用户反馈Codex额度仍消耗较快时，再评估外层续等/重复更新/消耗归因及断线取消等问题。完整待办见spec/TERMINAL_RETURN_DRAFT.md末节；不自动派任务、监控或重跑测试。

- 最终重连验收已通过：一次零等待调用命中#36，任务/成果引用正确，新截断字段及4项实测计数字段生效，旧硬编码计数不存在；工具0.094秒/客户端5.538秒/HTTP1/外层续等0。无需再次重连或重复10分钟测试。

- 十分钟等待切片已完成实现及独立复核：DeepSeek #31/#32/#34，Kimi #33/#35/#36；最终PASS_WITH_LIMITATIONS，成果已收取。默认/最长600秒，目标状态提前返回。27项正常管道测试通过；真实Codex600秒请求与立即返回已验证。
- 任务摘要最多20条，完整ID列表O(N)，不承诺整体输出固定上限；已修正开发报告过度表述。日常只指定当前task_ids，不查询全项目历史。
- 本轮#36复核326.415秒，新增规模测试墙钟22.021秒。此前真实Codex600秒调用客户端604.269秒、内部HTTP123、外层续等8次；不能宣称零模型回合或量化节省。
- DeepSeek/Kimi当前均已核对--timeout 3600；Codex talk MCP超时配置660秒。最新返修输出已重连验证；取消/断线端到端恢复未实现或验证，不另起会话替代原会话。
- 当前分支 `codex/terminal-return-codex` 已获明确目标推送授权并完成推送；未合并 `main`。完整记录见 PROGRESS_HISTORY 与 spec/WAIT_TEST_RECORDS.md。
- 前端两项已落地，用户页面验收仍可继续；角色列表输出修复已加载。下一步不自动扩展新切片。

## 默认协作与消耗控制

- 前端Kimi开发/DeepSeek审查，后端反向；Codex只管范围、摘要、分歧、文档及Git收尾，不重复完整代码审查/自测。
- Codex默认不操作浏览器；页面由用户验收。工具结果先过滤，只输出状态或短报告；同一会话不重复读取简报/模块全文，恢复仅补读进度和变化。等待不读diff/日志/截图，长任务放慢状态查询。
- 昨日角色历史原始输出约64718 tokens且夹带旧日志，后来被截断；不能等同于实际计费或准确解释30%额度。此次在调用过滤之外落实工具端默认有界输出。

## 验证与边界

- 角色输出：Kimi独立运行tests.test_talk_task_tools 8/8、tests.test_talk_terminal_mcp 8/8；含大量历史实例+超长错误日志、两条来源、最新实例/空实例、catalog stdio兼容。DeepSeek沙箱管道限制导致的缺测已被独立审查补齐。
- 当前Codex TALK stdio工具路径已修正；旧pi扩展未改，当前不使用pi。在线状态仍沿用既有语义，历史idle/busy误报问题另列，不能仅凭available判断在线。
- 驻留旧代码的TALK MCP进程需要重连后加载修复；本轮未重启用户服务、未清理数据库历史、未改密钥。
- .tmp、预览、日志、数据库不提交。用户级DSH配置未由Codex修改。

## 下一步

1. 本版已获用户同意先使用；沿用当前等待和分工，不主动启动消耗优化。
2. 用户体验现有页面与委派流程；必要时另行讨论取消/断线恢复或严格分页，不默认扩大范围。
3. 指定分支的清理与推送已完成。下一功能方向沿用 BRAINSTORM_NEXT.md：先明确会议轮次与原终端请求的最小往返合同，再提出独立切片；本轮未启动开发。

恢复指令：`继续项目`。完整过程见PROGRESS_HISTORY.md。
