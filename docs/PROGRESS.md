# Project Progress

Updated: 2026-09-13 (Asia/Shanghai)

## 当前状态

- 当前Codex角色为决策Agent。本片基线d9a32fe，分支codex/terminal-return-codex；左侧任务列表新增执行耗时已实现，Kimi #54开发、DeepSeek #55独立复核PASS，两项已收取。
- 每条任务显示耗时HH:MM:SS：claimed_at到finished_at，不含排队与待收取；执行中每秒原地更新，结束固定，缺有效起止显示—，小时不回卷。重试只显示最近一次执行，不累计全部尝试。
- 纯前端，复用API已有时间字段；单timer不增加网络或重绘，任务状态仍由既有5秒轮询更新。结束后可有最多约5秒的固定值显示延迟；澄清/人工决策阶段无有效结束时间显示—。
- 独立48项定向回归、73条时间样本交叉比对和11种状态门控检查通过。实际页面视觉待用户验收；Safari/WebKit微秒解析和真实HTTP端到端未测，不宣称已完成这些验证。

## 验收与下一步

1. 刷新当前TALK页面进入项目任务列表，必要时Ctrl+F5加载20260913-task-duration资源。使用已有登录密钥，不新建账号或模型探针。
2. 查看已结束任务耗时固定、运行项每秒递增、未领取项显示—；检查列表换行、筛选与点击。普通任务完成待收取期间耗时不应增长。
3. 本片文档/差异检查后统一提交推送当前分支，暂停扩展，等待用户视觉验收。无需重跑完整测试或长等待实验。

## 等待与交付流程

- 项目外层等待目标600秒仍以宿主能力/更高优先级指令允许为前提；本轮实际exec外层yield=60000ms，内部wait预算600秒。4次请求内部耗时600.032/83.032/600.032/47.734秒，持续主控等待到复核收取完成；不能宣称验证了外层600秒或节省token。
- 此前短程yield600000参数未报错、53.6秒返回；连续600秒阻塞能力未证实。用户级MCP超时660秒、执行者预算3600秒保持，未改全局配置。
- #50/#51完整结构化JSON入服务器与新MCP摘要真实入口已验证；#52/#53调研已收取，二进制原句命中不证明当前加载路径，替换配置能否覆盖开发者层指令未证实。无外部自动唤醒、断线补领或取消立即生效保证。
- 主控按TASK_WORKFLOW摘要读取，必要时按稳定引用补读；schema匹配只是自报，不替代独立复核。前端Kimi开发/DeepSeek复核，后端相反；Codex维护正式文档/Git，不重复全量探索。

恢复指令：继续项目。完整证据见PROGRESS_HISTORY.md。
