# 统一终端成果回传方案（Codex首片已授权探索）

日期：2026-09-11。初版仅设计；用户随后授权先推进Codex首片，DeepSeek开发探索、Kimi独立复核。覆盖Codex、官方Kimi Code、DeepSeek Harness作为发起终端的场景；模型名称不决定终端职责。

## 已验证与目标

- 当前会话实际talk_list_agents返回1322字符，三个角色各1条实例，仅id/runtime/status/current_task_id/last_seen_at/pid，没有last_error和历史数组；修复已加载。
- 等待/重连/状态检查由普通程序处理，不启动模型回合。只在成果提交、失败或需澄清时投递有界摘要；模型调用量在等待期为0，不承诺整体额度降低的具体比例。
- 结果必须回到发起任务的具体会话，不能只发给agent:codex等成员；同成员多窗口必须隔离。保持人工收取/质量门禁，收到通知不等于任务completed。

## 架构建议

1. TALK通用层：登记已认证终端会话绑定、派发来源、任务状态事件与可补领通知。任务状态及事件持久化应一致，避免完成后通知丢失。
2. 本地终端连接器：模型之外的常驻程序负责订阅/长轮询、离线补领、去重、持久化收件箱和会话忙闲排队。复用通用bridge基础设施，不将规则写死在codex_bridge.py。
3. 运行时适配器：Codex、Kimi、DSH分别实现绑定/验证会话、提交摘要、检测接收确认及忙闲。只有能力验证通过才标为自动续接；普通MCP通知不自动等于客户端会触发模型。
4. MCP入口：派发时由可信连接器附加来源会话绑定；模型无需反复传入完整历史。保留查询/人工收取作为诊断及降级路径。

## 最小数据与流程

- 来源绑定：服务端认证成员、project_id、endpoint_id、runtime、原生session/thread_id、本地工作区标识及连接代次。一个member可绑定多个endpoint。会话ID不能替代鉴权；跨项目/跨账号投递必须拒绝。回调目标只用本地已登记适配器，不接受成果正文指定的命令/任意URL。
- 派发：来源先绑定，创建任务时关联origin。离线/无法自动续接时明确返回manual能力，不默默另建会话。
- 执行：原执行bridge提交成果。TALK生成含event_id、task_id、状态版本、result_message_id的通知，不附带整段对话/旧实例日志。
- 等待：本地连接器在模型回合之外等待，允许程序轮询但无模型推理。断线保留游标并补领；结果拉取继续按任务权限校验。
- 投递：给原会话有界摘要及结果引用（建议每项最多2KB；正文按需取）。忙碌时入队，默认不打断用户。多项结果可合并通知，不能串项目。
- 确认：持久化收件箱接收与运行时接收分开记账；按endpoint+event_id去重，采用可重试投递，不承诺跨进程崩溃下天然严格一次。运行时无幂等确认时不能盲目重发启动模型，须对账或转人工待确认。
- 通知ack只说明送达；审查、接受和collect_result仍走原工作流。自动续接只能推进已授权范围，不能自动增加切片/跨越验收。

## 三种终端的能力验证

|终端|候选接入|未确认项|
|---|---|---|
|Codex|官方App Server thread/resume、turn/start|能否经受支持连接到用户当前Desktop/CLI原会话；禁止用新进程新会话冒充回传|
|Kimi Code|原生会话接口或受支持的连接器入口，待核验|当前交互会话能否安全追加输入、忙闲与接收确认|
|DSH|Harness插件/会话入口，待核验|web/headless入口差异、原会话定位与触发方式|

官方Codex参考：https://learn.chatgpt.com/docs/app-server 。文档证明API支持已管理会话恢复/新回合，不证明本机现有Desktop可由外部任意接入。Kimi和DSH本轮未完成接口探测。

## 推进顺序（仅建议）

1. 先做三种终端原会话回传能力验证，使用固定测试通知，不开发业务、不自动派发下游；区分支持/不支持/需TALK启动包装器。未能接回当前Desktop不能算Codex端完成。
2. 确认接口后落最小来源绑定+持久通知+单终端投递链；共用接口为另外两种终端预留，不绑定模型身份。
3. 接入其余适配器，再验证断线补领、重复事件、忙碌排队、同成员多会话隔离、关闭会话、权限变化、通知与收取分离。
4. 指标：等待期模型启动次数=0；一次新成果正常情况下触发一次续接；重复事件不重复触发；真实结果回原会话。记录模型实际用量（如有），不用5小时额度百分比推测token。

没有修改功能代码、没有启动服务/新会话，没有派发开发任务。此方案待用户讨论确认。

## 用户新增硬性约束与当前首片

- 会话信息必须准确连续。不能只靠模型摘要传递：任务原文、补充要求、执行版本、审查与成果须有可追溯的关联；摘要携带原记录引用，读取时再次校验权限/版本。
- 任何阶段断线须保留未交付状态并支持补领；无法确认模型已接收时标记待对账，不谎报送达，不自动新建会话，不盲目重复触发模型。
- #28交DeepSeek先验证当前Codex原会话受支持入口。无法确认入口则只交付证据与阻塞选择，不擅自做替代会话或不可用适配器；确认可行后才允许独立opt-in小实现和隔离测试。不得触碰当前会话或启动真实模型。
- Kimi随后独立核查可行性证据/连续性合同/实际测试；不能将假transport测试当作当前Desktop回传已通。跨终端扩展及后端协议/数据库变更不在当前首片。

## Codex首片结论（#28探索，#29独立复核）

- 判定：PASS_WITH_LIMITATIONS，仅接入能力核验完成，尚未实现或验证当前Desktop原会话自动回传。
- 已验证：本机CLI 0.144.4的daemon命令返回仅Unix支持；本地协议schema具备thread/resume、turn/start（clientUserMessageId）、turn/steer（expectedTurnId）和会话状态字段。--remote需要可连接的endpoint。协议能力不等于已有Desktop原会话可外部接入。
- 纠正#28报告过度结论：应为“未验证存在受支持的当前Desktop接入入口”，不得写成“不存在入口”。config缺键不能证明无内置连接；proxy仅Unix socket与过载错误码缺少本地证据，不作为本片判定依据。报告证据实际位于.tmp/codex-terminal-return/，不是.tmp根。
- #29确认：另起宿主resume同一threadId没有被证明能保持当前UI同步与唯一执行者，禁止自动采用；连续性合同覆盖绑定/版本/断线/去重/busy/确认不确定，无明显缺项。
- 后续需用户选择：继续保留当前Desktop，暂采用结果通知/引用与人工续办；或接受另行设计由TALK管理的Codex入口，再验证受管理会话完整链路。后者不等于当前Desktop原会话已接通，必须明确迁移与历史保留方案。没有用户选择前，不建立替代会话。
- 本片没有功能代码、真实模型调用、服务重启、原会话写入、数据库/用户配置修改或历史扫描。没有声称零丢失/严格一次的实际端到端验证通过。

## #30同类工具调研后的优先路径

- 用户提供D:/claude-test/调研报告，DeepSeek只读调研并交付.tmp/terminal-return-research/REPORT.md；LLM-Wiki-Gist未读完，其它报告以相关章节为主，不声称全部源码通读。
- 报告所述共同结构：herdr服务端wait、pi进程内await、multica守护进程事件+程序轮询；CCB依赖其掌握的终端pane注入。这些是报告中的能力，不等于本机Desktop能力已证实。
- 优先考虑同一次MCP调用持续等待，结果返回后继续原会话；与模型回合结束后的外部主动唤醒是不同机制。前者可先保留Desktop验证，无需先迁移宿主。
- Codex定向核对当前wait_tasks已在普通Python循环内查询状态，但timeout硬限30秒。纠正调研建议的范围：新增服务端task事件/SSE不是首轮必要条件；先验证客户端长工具调用预算，再决定是否放宽等待上限及改进返回摘要。服务端事件可作后续效率优化，断线补领能力仍需任务级专项验证，不采信现有SSE就已保证任务通知可靠。
- 最小实验建议：专用无业务副作用的等待工具，验证当前客户端长调用超时、取消、断线以及结果是否留在原会话；目标预算300秒，必要时分段探测。未执行实验，未改变工具或服务。等待期间不产生新的模型查询回合，状态完成可提前返回；超时仅返回一次简短状态。不能把持续工具调用和回合结束后自动唤醒混为一谈。
- 下一步先讨论/验证上述工具等待方案，再决定原会话外部唤醒是否还必要。保留绑定、完整原文引用、任务版本、幂等与确认不确定的连续性约束。

## 5分钟等待实验（2026-09-11，部分验证）

- 使用已连接MCP，对已完成任务#30等待不会自然出现的clarification_requested状态；只读，无任务/服务修改。functions执行单元内部组合现有最长30秒talk_wait_tasks，运行305秒，9次请求均超时，末态仍completed；摘要回到当前会话。
- 宿主初次约1秒即返回running cell，Codex随后进行了5次functions.wait续等，且按当前运行环境要求发送检查点说明。因此证明的是执行单元可存活并回传，不是5分钟无模型参与，也不是单个MCP请求可持续300秒；不能据此承诺额度节省。
- 尚未验证：单请求300秒的客户端/stdio超时、取消、断线恢复、真实成果提前返回。下一步应以独立测试入口验证单请求长等待，避免直接改生产逻辑或将短请求组合误报为零回合等待。

## 2026-09-12 独立长请求探针已准备，待重连

- 用户授权再试一次，报告5小时剩余23%，本轮不重复组合等待。新增本地临时探针.tmp/mcp-long-wait-probe/server.py（标准库stdio MCP，不连接TALK/网络/模型）。long_wait_probe参数seconds=0..300、marker，等待后仅返回标记/耗时。
- 本地子进程seconds=0验证initialize、tools/list、tools/call通过；不是实际客户端300秒测试，也未验证取消/断线。
- 用户级C:/Users/Administrator/.codex/config.toml新增mcp_servers.talk_wait_probe，tool_timeout_sec=360；现有MCP配置/密钥保持不变。官方配置参考https://developers.openai.com/codex/config-reference/说明默认单工具超时60秒。
- 当前工具目录未加载新工具，需用户重连MCP或重启Desktop并回本对话。下一步只发现long_wait_probe并发起一次seconds=300、唯一marker请求，不读角色历史，不用9次短等待替代；如宿主外层仍yield须记录，不能宣称零模型回合。记录实际请求成功/超时、回同一会话和宿主续等次数。
- 探针暂留本地.tmp和用户级配置，不提交。实验完成移除独立配置块（不修改talk/node_repl），不要误删其它文件。仍未修改生产等待上限。
