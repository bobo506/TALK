# TALK 后续开发安排：多终端主控与桌面工作区

更新时间：2026-09-18。状态：N1 已完成并推送；L1-1 入口准备已收取；#64–#69 已完成 Kimi 受信隔离目录下单任务真实主控闭环（含人工独立验收/唤回），DSH入口准备经#70–#73收取，ACP零模型同会话恢复与48项回归经#74–#79通过，真实模型单任务闭环经#80–#84通过（有人值守）；WorkBuddy桌面两模型经#90/#91完成有人值守单任务闭环。项目级开发要求REQ-1后端/工具已完成并经#93独立复核；REQ-2前端经#94–#97开发/返工/独立复审通过，用户已按选定范围验收（跳过项见验收指南）；ROLE-1经#98/#99复核及用户页面验收通过。统一维护本文件的跨模块排期；群聊细节保留在 BRAINSTORM_NEXT.md，旧回传探索保留在 TERMINAL_RETURN_DRAFT.md。

## 已确认的产品边界

- 保留当前 Codex Desktop、Kimi Code、DeepSeek Harness 等原生 Agent 运行环境。TALK 负责协作记录、工作区选择和本地服务生命周期，不重新实现模型推理循环或替代原生终端。
- 正式使用目标为 Windows 桌面程序：点击应用→启动 TALK 服务（不自动启动任何角色）→打开承载现有 Web 页的应用窗口→新增/加载工作区→在角色列表手动启动或停止该工作区角色→关闭应用时退出本应用管理的所有工作区角色服务与 TALK 服务。
- 工作区映射到稳定 project_id 与规范化本地目录；既有项目/任务/Hall持久化继续复用。逻辑按项目、任务隔离不等于已在每个目录独立存数据库，是否需要目录内导出/迁移另行确定，不自动改现有数据布局。
- A工作区的Kimi与B工作区的Kimi是独立进程/会话/目录上下文；身份显示可同名，运行绑定必须能区分工作区、角色、实例与原生会话。相同隔离要求适用于DeepSeek和Codex。
- 默认主控被动：派发后结束本轮，由用户通知继续。主动模式保持等待并自动推进复核/收尾，等待中仅最小续等；仍遵守宿主指令，不承诺零模型续行。切换按钮不负责唤醒已结束的外部Desktop对话。
- 任务名称目标为“编号-任务名称”，例如58-主控模式切换，不含项目前缀，项目由顶部选择器区分。

## 项目级开发要求（2026-09-18：后端/工具与前端完成，用户按选定范围验收通过）

- 角色列表提供项目级开发要求编辑区，给主控读取，描述各角色职责与交叉复核约定；示例为 Kimi 负责文字/交互/前端、DeepSeek 负责后端，彼此独立复核。
- 用户明确只保存最新内容，不做版本号或历史记录；主控派发新任务前读取并写入相关任务包，已派发任务继续按原包执行。
- 实现后替代 AGENTS 中对应职责分工的双重维护，仓库代码规范仍保留；页面要求不能覆盖外部宿主系统指令。REQ-1已实现持久化/API/CLI与MCP读取、派发时正文快照；REQ-2编辑区已完成并经#97复审，AGENTS固定模型分工去重，改为派发前读取项目要求；角色分级与仓库规范继续有效。尚未写入真实项目要求，需用户配置并验收。
- 用户后续确认先继续终端验证：Kimi完成后DSH、WorkBuddy，再安排本需求；按后端/工具与前端编辑划分切片，不将需求塞入已冻结的终端验证实现。

## 当前可复用能力与缺口

| 项目 | 已有依据 | 仍需完成 |
| --- | --- | --- |
| 项目/任务记录 | Project已有project_id/project_root_path；ProjectAgent按项目和member存资料索引；页面按project_id加载任务 | 原生目录选择、注册/加载工作区完整交互、路径迁移/重复注册处理 |
| 任意角色当主控 | 根负责人下拉无模型限制；人工授予may_delegate等预算后可拆分；通用终端MCP已存在 | Kimi/DeepSeek各入口完整主控流程实测、角色授权与工具可用性一致性；不能仅改标签 |
| 角色执行 | 已有CLI bridge与workdir，当前Kimi/DSH作执行者已用过 | AgentInstance当前无明确project_id，worker列表主要按target_member_id筛选；多工作区任务领取、会话恢复和状态归属须进一步隔离 |
| 桌面化 | FastAPI/Web UI/健康检查已有 | 桌面容器、打包依赖、目录选择、本机受限启动接口、进程管理及关窗退出 |
| 主控模式 | AGENTS已有被动默认/最小续等约定 | 持久化模式、前端按钮、工具返回模式版本、读取/生效节点与有效状态显示 |
| 标题和耗时 | 任务编号、执行耗时已经存在；耗时#54/#55实现复核已完成 | 标题各入口格式一致性；耗时页面视觉仍待用户确认 |

上述源码核对仅作规划依据，不代替隔离与终端验收。尤其不能因ProjectAgent已有project维度就认定执行进程已隔离。

## 多终端主控：先验证通用能力，再决定适配

“模型/业务角色/终端产品/具体会话”是不同维度。WorkBuddy里使用Kimi或DeepSeek模型，不等于运行Kimi Code CLI或DeepSeek Harness；模型服务、工具能力、提示词、权限、会话恢复都可能不同，不能承诺同等表现或共享认证。

| 发起入口 | 当前依据 | 本轮判定与后续验证 |
| --- | --- | --- |
| Codex Desktop→TALK | 当前已实际用于派发、复核、收尾 | 作为现有基准，默认人工唤回；不重跑十分钟探针 |
| Kimi Code→TALK | 官方支持stdio/HTTP MCP；已有执行bridge | 验证本人终端身份、目录/权限、委派、读取完整交付、独立复核、收取汇总；没有完整主控验收结论 |
| DeepSeek Harness→TALK | 已有DSH执行bridge；通用TALK工具可复用 | 先核验本机Harness工具接入方式及真实模型能力，再跑同一主控验收；不能把执行任务成功等同主控成功 |
| WorkBuddy＋Kimi | 官方支持MCP与自定义模型 | 5.5.6桌面kimi-k3经90闭环通过；用户级MCP及专用身份，跨工作区/无人值守未验收 |
| WorkBuddy＋DeepSeek | 同一WorkBuddy宿主可优先复用一套MCP配置方案 | deepseek-v4-pro经91受控补测通过；与Kimi共用MCP和专用身份，不按模型复制TALK后端 |

通用MCP兼容时，优先只提供配置模板和角色使用合同，不为每个模型重写一套服务端。不同终端可能需要各自的启动/会话/退出适配；只有发现真实协议或能力差异再开发薄适配器。主控发起能力与“页面能自动启动该终端进程”分别验收。外部原会话自动回传不作为首版主控的前提，保留人工唤回路径。

每条主控链路最小验收：指定工作区和身份→人工授予有限协调职责→派一项无破坏性工作→由不同角色复核→原发起会话读取/收取结果并汇总；验证无串项目、无自派递归或重复主控、无越权新增预算。身份/决策权限按明确项目与任务授权，不因选择模型而自动升权。先验单工作区，完成隔离层后补双工作区组合。

## 桌面与工作区合同

1. 新增工作区：通过原生目录选择器选择已有本地目录并登记为项目；不是复制仓库或隐式git init。可选创建新目录另行细化。复用现有项目识别，目录规范化与稳定ID分开，避免同目录重复注册；移动/丢失目录明确提示重新关联。
2. 加载工作区：加载该项目任务、Hall、角色资料及运行状态。选择项目不等于修改已运行进程的cwd，不把A会话改名后重用于B。
3. 角色实例：每个项目角色显式绑定cwd、实例ID、原生会话ID/恢复参数和任务范围；可复用运行器安装和授权配置，但项目记忆/上下文/输出关联隔离。每个新任务究竟新建还是恢复会话按原生运行器能力确定，跨项目绝不恢复同一会话。
4. 领取隔离：项目范围要进入任务发现、服务端领取校验与实例关联，不只前端过滤；旧成员ID/实例/任务兼容迁移单独设计，不直接破坏既有记录。调度并发限制也核对是否错误地把A/B同名角色视为同一运行槽。
5. 启停：页面明确显示未启动/启动中/运行/停止中/失败；重复点击不重复启动，忙碌时停止的中断/租约/任务结论如实记录，停止不等于成功。控制来自本机受限入口，不能让普通远端页面凭任意命令字符串启动程序。
6. 切换工作区：建议初版保留已启动的A项目服务，B项目只显示/控制B；A/B能并存。用户尚未明确切换时是否自动停旧服务，因此该建议在实现前确认，不能暗中终止在途任务。
7. 关窗：正常关闭应用停止接新任务，协调停止全部由本应用启动的角色与子进程，再停止TALK服务并关闭窗口；忙任务做必要落盘与有限等待，超时中断必须可追溯，不能伪报完成。验收还需覆盖崩溃/异常退出、启动失败回滚与残留进程恢复。只清理本应用拥有的进程，不能按kimi/python等名称全局杀进程，也不关闭用户独立打开的Codex Desktop/WorkBuddy。
8. 桌面程序启动不得悄悄接管或关闭此前手动启动的同端口服务。首版需定义单实例、端口冲突和所有权策略；标准路径由桌面程序启动并拥有服务。技术壳与打包工具留专门小型选型验证，尚未选定框架。

## 建议切片顺序

2026-09-14 用户调整：先完成基本业务功能，群聊前移到第7阶段；核心功能验收后再启动桌面封装。工作区/角色的服务与普通Web交互在前置阶段完成，原生目录对话框和窗口生命周期留给桌面阶段。

| 顺序/切片 | 内容与交付 | 依赖/验收重点 |
| --- | --- | --- |
| 1 N1（开发/复核完成） | 统一任务标题“编号-任务名称”；#58–#61 已收取，页面待验收 | 小片先交付；以实际task.id为准，Web/MCP/重试不重复前缀；历史标题是否批量改名另确认 |
| 2 L1 | Kimi Code、DSH主控入口验证；Kimi单任务闭环已实测；DSH入口准备已收取，ACP零模型恢复及48项回归通过，真实模型单任务闭环经#80–#84通过（有人值守） | 分终端一次一片；先配置复用MCP，给支持/缺口清单，禁止把工具发现当闭环通过 |
| 2 L2 | WorkBuddy两模型单任务接入已验证（90/91） | 用户操作下原对话受控收取通过；项目级自动加载、多工作区和无人值守未验收 |
| 3 C1/C2 | 主控模式存储/工具状态，再做前端按钮 | 项目级默认被动、明确当前主控身份与模式版本/有效状态；同项目多主控不争抢；结束会话切主动仍人工唤回 |
| 4 W1 | 工作区注册/加载与目录绑定合同 | 复用projects，不混淆选择项目与创建原生会话；先后端/兼容再UI，原生目录选择接桌面桥 |
| 5 I1/I2 | 项目范围的角色实例、任务领取与原生会话隔离 | 协议/数据变更单片暂停复核；A/B同名角色同时运行，不串目录/任务/历史，不重复执行 |
| 6 R1/U2 | 本地角色进程管理、统一启动/停止合同及Web角色启停入口 | 先一个原生运行器验通，再适配其余；包含退出、子进程清理、忙任务中断、启动回滚与日志归属 |
| 7 G1起 | 群聊会议轮次、回复提醒、原终端主持与总结 | 沿用BRAINSTORM_NEXT已确认方向；有隔离/终端能力基础后推进，自动外部唤醒另列可选探索 |
| 8 D1 | 桌面壳：仅自动启动TALK，承载Web，关窗清理 | 基本业务功能（含群聊）验收后开始，依赖进程所有权合同；不开角色自启动，单实例/端口冲突/正常与异常退出/记录持久化 |
| 9 U1 | 原生目录选择与已有工作区/角色页面的桌面接入 | 接W1/R1/D1；复用前置Web功能，补齐本机目录选择和窗口交互，不把基础启停功能拖到封装阶段 |
| 10 A1 | 桌面正式使用里程碑验收 | 冷启动→A启动Kimi→B启动另一Kimi→分别派任务→切回看记录→关窗确认所属服务全退→重开记录仍在；再覆盖DSH/Codex等支持运行器 |

每行可拆若干小片，不是一次派发整批。默认前端Kimi开发/DeepSeek复核，后端及进程管理DeepSeek开发/Kimi复核；Codex负责范围、分歧和文档/Git收尾。主控能力验证可以临时授予本次协调职责，不全局改所有角色等级。跨协议/数据库/桌面权限切片完成后暂停确认；页面效果由用户验收。默认派发后结束主控等待，由用户通知完成。

## 保留的其他待办与暂缓项

- ROLE-1角色说明统一及参与任务执行耗时已由#98/#99开发与独立复核通过，2026-09-18用户页面验收通过。自由文本业务角色命中JS原型键时的异常显示登记低优先级加固（建议Map/自有键检查），不影响现有lead/dev/reviewer，暂不扩片。

- **独立任务的实际工作分类（2026-09-18，用户明确暂缓）**：当前独立顶层开发、检查/复核、测试、返工继续省略 `task_kind`，使用默认 `general`，真实职责写在标题和任务包，不改现有派发合同、不迁移历史任务。未来若需按实际工作统计或展示，再评估独立顶层任务支持 `development / review / test / rework`，或将工作分类与流程类型分离；需检查父子关系、授权、审查门禁、API/MCP/界面兼容，不能仅更换标签或直接传入现有子任务类型。此项仅记录，不启动开发。

- 群聊轮次隔离、回复提醒、总结关联原发起会话仍待实现，保留原产品方向。
- 取消/断线恢复、租约过期与结果去重等可靠性项，先完成桌面退出/项目隔离必需部分，其余随后推进；不能认为当前600秒同步等待已覆盖这些能力。
- #56 model_instructions_file运行时A/B未完成，作为可选研究保留，不阻塞产品路线；不再以降低推理强度或计费归因实验作为本批工作。
- 大规模列表/输出分页、消息虚拟滚动等旧增强项按真实规模触发，不抢占本批顺序。
- ROLE-1角色参与任务耗时已获用户页面验收；其它早期页面验收状态不由本次结论推断，不重复全量代码审查。

## 官方能力来源（2026-09-14核对，非本机验收）

- Kimi Code MCP：https://www.kimi.com/code/docs/en/kimi-code-cli/customization/mcp.html — 支持外部工具接入；不证明已配置好TALK主控闭环。
- WorkBuddy MCP：https://www.codebuddy.cn/docs/workbuddy/From-Beginner-to-Expert-Guide/Function-Description/MCP-Guide — 提供MCP配置；不证明与本机TALK已连接。
- WorkBuddy模型配置：https://www.workbuddy.ai/docs/workbuddy/From-Beginner-to-Expert-Guide/Function-Description/Model — 支持自定义模型；具体Kimi/DeepSeek模型及账户可用性仍需本机核验，不能等同原生运行器。
