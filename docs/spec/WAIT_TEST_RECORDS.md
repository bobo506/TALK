# 等待机制测试记录

计数口径：Codex状态查询为模型发起的talk_wait_tasks/get_task/list_tasks调用，单独计数；functions.wait为外层续等，内部HTTP请求不是模型查询。派发/回复/成果收取另记。次数不能直接换算计费token；缺少证据写未知。

| 测试 | 时长 | MCP等待调用 | Codex其它状态查询 | 外层续等 | 内部HTTP | 结论 |
|---|---|---|---|---|---|---|
| 早期组合等待 | 305秒 | 9（程序在一个执行单元内组合） | 未完整统计 | 5 | 未统计 | 非单次长MCP，不可当作9个模型回合 |
| 单个探针 | 300秒 | 1 | 0（该探针等待期间） | 4 | 0 | 单调用通过，外层主动yield，非零模型回合 |
| 600秒生产等待 | 待测试 | 待记录 | 待记录 | 待记录 | 待记录 | 未验证 |

每次新测试另记：日期、代码版本、客户端超时、请求时长、实际耗时、任务ID/状态/成果引用、返回原因、以上各项计数、模拟或真实时间、错误/取消/断线及限制。实际测试填写，不能用预计值代替。

## 本轮开发协调（独立于等待性能测试）

- 基线be45b2d，开发任务#31。
- 派发3次：2次HTTP422校验拒绝，1次成功；未创建重复任务。
- 截至派发完成：Codex状态查询0次、外层续等0次、成果收取0次。后续实际调用继续追加，不将此中间数当最终数。

- 本轮暂停时累计：Codex MCP状态查询2次（wait_tasks一次、get_task一次），外层functions.wait续等1次，成果收取0次；另有本地报告存在性检查1次、clock.sleep四次（每次60秒，不查询任务但会使模型续行）。不能将本轮描述为零模型等待或正式600秒测试。
- 曾尝试创建5分钟持续自动检查，自动审批拒绝，未创建任何自动化；理由是持续额度消耗且缺少本次明确授权。未采用绕过方式。开发仍在执行，暂停本轮协调，后续从#31恢复，不重复派发。
- 用户级talk工具超时已设660秒，TOML比较确认其它配置保持不变；尚未重连，当前工具仍旧版。正式600秒测试尚未开始。

- 用户询问进度后追加get_task查询1次：协调阶段累计MCP状态查询3次，外层续等仍1次。#31返回failed/failed，result_message_id=2507，完成时间2026-09-12T01:09:51.010842；结果仅说明Harness bridge未返回结果，失败原因和代码完成度尚未核验。

- 失败诊断新增get_task一次，协调累计MCP状态查询4次（wait一次、get三次），外层续等1次。#31执行时长600.10秒、提示匹配bridge timed_out；本地证据为26 tests/5 errors及5秒真实stdio超时冒烟，非600秒验收。无证据认定用户另一DSH窗口造成失败。

- 60分钟续接轮：派发2次（1次related_task_ids不适用于general的422拒绝，1次成功#32），get_task查询1次确认running；外层续等0次。开发协调累计MCP状态查询5次，functions.wait累计1次。另有一次只读SQLite查询失败（根talk.db无tasks表），不是有效任务状态证据；进程验证是运行环境检查。生产600秒测试仍未验收。

- 用户要求再测一次：前置检查get_task(#32)一次，仍running/in_progress，DEV_REPORT尚不存在；当前已加载MCP说明仍最长30秒。未发起600秒调用，避免将旧版截断当长等待通过。累计开发协调MCP状态查询6次、外层functions.wait1次；本轮性能测试调用0次。待开发暂停、独立复核及MCP重连后实测。

- 用户再次询问#32完成状态：新增get_task查询1次；开发协调MCP状态查询累计7次，外层functions.wait仍1次。本次为状态检查，非600秒性能测试。

## #33独立复核与最终测试准备

- 本轮list_tasks(agent:kimi)一次确认没有重复审查任务；collect_result(#32)一次；delegate(#33)一次成功；get_task(#33)一次确认运行。累计协调状态查询9次（此前7+本轮list/get各1）、functions.wait仍1次；成果收取累计1次。另clock.sleep两次各60秒，不是任务查询但仍有模型续行。
- #33创建07:06:09.595165 UTC、领取07:06:36.178739 UTC，排队26.584秒；复核耗时待完成时间，不估算。
- #32测试证据：定向4项4.330秒，全量26项29.054秒；600秒测试工具内600.031秒、客户端600.516秒、包含启动收尾墙钟601.531秒（01:38:47.967316至01:48:49.498407 UTC）。提前返回工具内22.75秒、客户端24.016秒、含启动收尾24.998秒（01:42:11.192605至01:42:36.190303 UTC）。两项真实测试重叠运行，不能相加作为总墙钟。
- 以上是开发者已产出的隔离程序证据，等待Kimi独立核验，非本轮Codex已重跑。当前已加载MCP仍30秒，正式600秒客户端调用尚未发起；需要重连。不将开发者无模型驱动脚本声称为本对话零模型回合。

## 重连后本对话实测（2026-09-12）

- 新工具描述已为默认/最长600秒。查询#33一次：PASS_WITH_LIMITATIONS，排队26.584秒，实际复核321.091秒（07:06:36.178739至07:11:57.269819 UTC），独立26项正常管道测试约32秒、fallback=0。收取#33一次。兼容问题须返修，已派发#34一次。
- 提前返回：对已完成#32设置600秒、状态completed；单次MCP调用，工具耗时0.016秒，客户端4205毫秒，matched、原任务与成果引用一致，HTTP1/轮询1，外层续等0。仅验证已有成果立即返回，不是中途提交场景。
- 完整超时：对#34仅发起一次600秒MCP等待；工具elapsed_seconds=600，客户端604269毫秒，timeout，HTTP123/轮询123。functions.exec yield_time_ms=60000，外层functions.wait实际8次；未用短MCP拼接。返回#34仍queued/assigned，不能声称返修运行或完成。
- 本轮协调get_task1次，累计协调状态查询10次；另正式性能测试wait2次单列，故累计所有MCP状态/等待调用12次。历史外层functions.wait1次+本轮8次=9次。collect_result累计2次。硬编码query_stats.wait_tool_calls不作为计数证据，上述来自实际调用轨迹。
- 结论：当前Codex真实连接单次600秒请求及已有结果立即返回均已通过；外层仍有模型续行，未验证零模型消耗、节省比例、取消与断线恢复。工具生命周期通过不等于兼容返修已完成。

- 用户重启后查询#34一次：running/in_progress；协调状态查询累计11次，另正式性能测试2次，合计13次；外层续等累计9次不变。进程只读核对显示未显式设置timeout，仍默认600秒；没有打断运行中的任务。

- #34成果查询新增get_task1次，协调累计12次+正式性能wait2次=14次；外层续等累计9次不变。收取#34一次（累计3次），派发Kimi返修复核#35一次。#34执行559.230秒，最终27tests40.650秒/墙钟42.460秒；前一次27tests41.082秒/墙钟44.448秒，两轮非长等待测试。未重跑600秒；返修最终验收待#35。

- #35失败诊断get_task1次：协调累计13次+性能wait2次=15次；外层续等9次不变。Kimi整轮执行600.116秒被超时，启动参数确认默认600秒；未提交复核报告，未重派任务。

- 两个bridge重新启动后，只读确认均3600秒；派发Kimi续接复核#36一次成功。此轮截至派发未发起任务状态查询/外层续等，既有计数不变。

- 最终#36查询1次：协调累计14次+性能wait2次=16次，外层续等累计9次；collect_result累计4次。#36执行326.415秒，新规模补测墙钟10.457秒和11.564秒，合22.021秒；沿用正常管道27项36.059秒，不重复套件与600秒。最终PASS_WITH_LIMITATIONS已接受并收取，O(N) ID索引限制写入合同。
