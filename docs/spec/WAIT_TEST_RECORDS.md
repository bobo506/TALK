# 等待机制测试记录

计数口径：Codex状态查询为模型发起的talk_wait_tasks/get_task/list_tasks调用，单独计数；functions.wait为外层续等，内部HTTP请求不是模型查询。派发/回复/成果收取另记。次数不能直接换算计费token；缺少证据写未知。

| 测试 | 时长 | MCP等待调用 | Codex其它状态查询 | 外层续等 | 内部HTTP | 结论 |
|---|---|---|---|---|---|---|
| 早期组合等待 | 305秒 | 9（程序在一个执行单元内组合） | 未完整统计 | 5 | 未统计 | 非单次长MCP，不可当作9个模型回合 |
| 单个探针 | 300秒 | 1 | 0（该探针等待期间） | 4 | 0 | 单调用通过，外层主动yield，非零模型回合 |
| 600秒生产等待 | 待测试 | 待记录 | 待记录 | 待记录 | 待记录 | 未验证 |

每次新测试另记：日期、代码版本、客户端超时、请求时长、实际耗时、任务ID/状态/成果引用、返回原因、以上各项计数、模拟或真实时间、错误/取消/断线及限制。实际测试填写，不能用预计值代替。

## 本轮开发协调（独立于等待性能测试）

- 基线be45b2d，开发任务#31。
- 派发3次：2次HTTP422校验拒绝，1次成功；未创建重复任务。
- 截至派发完成：Codex状态查询0次、外层续等0次、成果收取0次。后续实际调用继续追加，不将此中间数当最终数。

- 本轮暂停时累计：Codex MCP状态查询2次（wait_tasks一次、get_task一次），外层functions.wait续等1次，成果收取0次；另有本地报告存在性检查1次、clock.sleep四次（每次60秒，不查询任务但会使模型续行）。不能将本轮描述为零模型等待或正式600秒测试。
- 曾尝试创建5分钟持续自动检查，自动审批拒绝，未创建任何自动化；理由是持续额度消耗且缺少本次明确授权。未采用绕过方式。开发仍在执行，暂停本轮协调，后续从#31恢复，不重复派发。
- 用户级talk工具超时已设660秒，TOML比较确认其它配置保持不变；尚未重连，当前工具仍旧版。正式600秒测试尚未开始。
