# UI-WORKSPACE-1 页面验证

- 日期：2026-09-07；范围：现有 TALK 项目的任务 / 角色双栏切片。
- final result: passed
- 结论范围：本切片的信息组织、关键交互和响应式检查通过，可以提交人工验收。没有宣称像素完全一致、完整无障碍合规或完整端到端覆盖。

## 视觉依据与证据

用户已确认的任务视图 `exec-892e6dff-e667-4a2f-a464-d3e2c37807dd.png` 与角色视图 `exec-8600ee7f-1024-48aa-959d-b073c3763c42.png` 为同一方案的两种模式。生成图保存在本机 Codex generated_images 目录。没有重新选择设计方向。

实际截图保存在本地 `.tmp/workspace-qa-20260907/`，不随公开代码提交：

| 证据 | 核对内容 |
| --- | --- |
| `task-final.jpg` | 1488×1056，任务 #15、已完成、主任务列表、成果与流转 |
| `roles-final.jpg` | 1488×1056，Codex 角色、职责、工作状态、历史任务 |
| `task-mobile.jpg` | 390×844，换行后的标题栏和上下排列的任务页面 |
| `roles-mobile-visible.jpg` | 390×844，滚动后的角色职责、交办按钮、任务表格 |

最终任务图与对应预览在同一次比较输入中展示；角色图也分别同输入展示后逐项核对。初次调整尺寸时捕获的 `task-first.jpg`，以及全页捕获存在空白的 `roles-mobile.jpg` 不作为最终证据。实际 DOM 读取、稳定视口截图和真实点击共同用于判断。

## 视觉检查

- 双栏分隔、左侧同级任务/角色切换、浅色背景、蓝色选中态和主操作沿用预览方向与既有品牌样式。
- 原始技术字段移出普通详情，保留中文状态、负责人、真实成果入口和流转。失败/取消/完成同时有文字说明，不能只靠颜色识别。
- 按真实数据保留历史标题和成员短名，没有把示例标题改写进数据库，也没有把 `completed` 一概写成独立人工验收通过。
- 手机标题栏曾受旧样式 `height:100%` 影响遮挡按钮，已修正并读取边界复测；390px 页面宽度与 scrollWidth 相同，角色表格与弹窗都可操作。769px 完整对话页面没有横向页面溢出。
- 焦点有可见轮廓，输入框有标签，角色任务表有列标题。弹窗 Escape 可关闭并回到“交办任务”按钮。
- P3 后续细化：字重、字号、边距和辅助图标没有逐像素复刻生成图；角色采用文字名称，未加入预览的字母头像。较长真实标题会换行，流转完整列出真实子任务与质量结论，因此信息密度高于简短示例。创建弹窗仍含高级术语，属于后续候选切片。
- 本轮范围内未遗留 P0/P1/P2 阻断问题；以上 P3 与后续范围交由人工验收评估。

## 功能验证

1. 主任务归组；任务搜索、进行中空筛选；角色搜索与切回任务时清除角色查询；切换空项目不显示上一项目的角色/任务。
2. DeepSeek、Kimi 角色切换后展示各自真实工作；从 DeepSeek“交办任务”进入弹窗自动选中 DeepSeek；未提交真实任务。
3. #15 完成态只提供成果与完整对话，不显示暂停/终止；当前 QA Tester 没有该 Hall 的访问权限，点成果得到可见提示，5 秒轮询后提示保留。
4. #9 成果正确显示提交的 Python 循环文本；进入完整对话后保留正确任务上下文。按原缺陷路径 #15→左侧 Hall #9，详情、流转、操作均是 #9，没有 #15 Review/Test 残留。
5. 子任务详情与主任务分别显示自己的上下文；“返回主任务”可用。角色任务入口可跳转到具体子任务。
6. 最终页面开发者日志未捕获控制台错误。启动的是已有后端与静态页面，没有要求其它角色 bridge 在线。

## 自动化与边界

- `node --check web/app.js`、`node --check web/workspace.js`：通过。
- `node --test tests/workspace_ui.test.cjs`：11 项通过，直接运行生产异步加载/动作函数，覆盖旧响应覆盖、跨项目/账号、失败清理、旧树动作拒绝和操作后的选择保持。
- Python 页面契约 2 项 + 里程碑人工验收与任务澄清/提交/收取 2 项：4 项通过，使用隔离测试数据库。
- 没有执行真实任务的暂停、取消、收取、创建、消息发送；没有测试真实账号之间的登录切换，相关异步保护通过自动化测试验证。没有重新运行全量后台测试、文件成果下载或屏幕阅读器专项测试。


## 验收后的显示尺寸调整

用户已认可双栏布局与内容，要求默认密度接近浏览器原来缩放 80% 的效果。桌面采用 0.8 显示比例，窗口宽度小于 701px 时保留既有移动端尺寸。

- 1488×1056：标题栏实际高度 56px（原 70），搜索框约 35.2px（原 44），标题行高约 33.6px（原 42）；工作台底边仍为 1056px，左右覆盖全宽。
- 交办弹窗实际宽 496px，遮罩覆盖 1488×1056 视口；角色入口与关闭按钮可用。
- 769px 角色列表与表格未横向溢出；390px 根缩放为 1、搜索框仍为 44px，标题栏操作完整可见。
- 页面契约 2 项通过。此次是 CSS 密度微调，未重跑任务状态与后端工作流测试。
- 截图保存在 `.tmp/workspace-density-20260907/`。原截图包含用户留下的详情滚动位置，因此比例结论同时采用 DOM 尺寸测量，未将不同滚动位置误认为尺寸变化。
- final result: passed
